# Copy this file to .env (which is gitignored) and fill in your key.
# The pipeline auto-loads .env via config.py; never commit real secrets.

# FRED macro data — optional. Without it, ingest/macro.py uses the keyless CSV endpoint.
# Request a free key at https://fredaccount.stlouisfed.org/apikeys
FRED_API_KEY=
